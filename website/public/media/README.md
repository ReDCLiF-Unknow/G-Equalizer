# Comparison videos

Drop the two clips here with these exact names (referenced by `src/components/Compare.astro`):

- `eq-off.mp4` — same recording, EQ off / flat
- `eq-on.mp4` — same recording, PUBG preset on

Keep them the same length and same source clip so the only difference is the EQ. `.mp4` (H.264) is the safest format for browser `<video>` support without extra encoding.
